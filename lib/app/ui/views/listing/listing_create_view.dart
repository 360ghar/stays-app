import 'package:flutter/material.dart';

class ListingCreateView extends StatelessWidget {
  const ListingCreateView({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Create Listing')));
}
